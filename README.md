# 📝 Task Manager Application

## **Overview**

The **Task Manager Application** is a Python console-based program that allows users to organise and manage their daily tasks. Users can **add**, **view**, **update**, and **mark tasks as completed**. All task information is stored in a **CSV file**, ensuring that tasks are saved and can be loaded whenever the program is run.

This project demonstrates the use of **fundamental Python programming concepts**, including loops, conditional statements, functions, file handling, and CSV file management.

---

# **Features**

* ✅ Add new tasks
* ✅ View all tasks
* ✅ Update existing tasks
* ✅ Mark tasks as completed
* ✅ Save tasks to a CSV file
* ✅ Load tasks from a CSV file
* ✅ Menu-driven interface
* ✅ Data is saved between program sessions

---

# **Python Concepts Used**

## **Loops**

The application uses loops to repeatedly display the menu until the user chooses to exit.

**Examples:**

* `while` loops
* `for` loops

---

## **Conditional Statements**

The program uses **if**, **elif**, and **else** statements to make decisions based on user input.

These statements are used to:

* Validate user input
* Navigate the menu
* Check task completion status
* Execute different program options

---

## **CSV File Reading and Writing**

The application stores task information in a **CSV (Comma-Separated Values)** file.

The program demonstrates:

* Reading data from a CSV file
* Writing updated data back to the CSV file
* Saving information permanently between sessions

---

## **Operators**

The project uses several different types of operators.

### **Arithmetic Operators**

* `+`
* `-`
* `*`
* `/`
* `%`

### **Comparison Operators**

* `==`
* `!=`
* `>`
* `<`
* `>=`
* `<=`

### **Assignment Operators**

* `=`
* `+=`
* `-=`

### **Logical Operators**

* `and`
* `or`
* `not`

---

## **Variables**

Variables are used throughout the application to store:
